import { DateBorderDirective } from './date-border.directive';

describe('DateBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new DateBorderDirective();
    expect(directive).toBeTruthy();
  });
});
